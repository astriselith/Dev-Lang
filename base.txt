{
	"package" = "com.dev.lang.base",
	"artifact" = "lang",
	"version" = "1.0.0"
}class Any {
	readonly var pointer: Int = 0;

	fun print < T | Stringifiable > (content: T) = ?;
	fun println < T | Stringifiable > (content: T) = ?;
}readonly class Array<T> | Indexable<Int, T> {
	fun length(): Int = 0;
	fun get(index: Int): T = null;
	fun set(index: Int, value: T) = ?;
}trait Assertable {
	fun isTruthy(): Bool;
}readonly class Bool;readonly class Char | Equality {
	fun code(): Int = 0;

	fun toUpper(): Char = '';
	fun toLower(): Char = '';

	fun equals(other): Bool = false;
}trait Indexable<K, V> {
	fun get(k: K): V;
	fun set(k: K, v: V);
}readonly class Float : Number;readonly class Int : Number;class Number | Addable<Number>, Subtractable<Number>, Multipliable<Number>, Dividable<Number>, Modable<T>, Relational<Number>, Equality {
	fun add(other: Number): Number = null;
	fun sub(other: Number): Number = null;
	fun mul(other: Number): Number = null;
	fun div(other: Number): Number = null;
	fun mod(other: Number): Number = null;

	fun relation(other: Number): Int = 0;
	fun equals(other): Bool = false;
}readonly class String | Addable<String>, Equality, Stringfiable {
	private readonly var chars: Array<Char>;

	fun charAt(idx: Int): Char = '';
	fun add(other: String): String = "";
	fun equals(other): Bool = false;
	fun toString(): String = this;
}readonly class Void;trait Stringifiable {
	fun toString(): String;
}trait Addable<T, R> {
	fun add(other: T): R;
}
trait Subtractable<T, R> {
	fun sub(other: T): R;
}
trait Multipliable<T, R> {
	fun mul(other: T): R;
}
trait Dividable<T, R> {
	fun div(other: T): R;
}
trait Modable<T, R> {
	fun mod(other: T): R;
}trait Relational<T> {
	fun relation(other: T): Int = 0;
}
trait Equality {
	fun equals(other): Bool;
}